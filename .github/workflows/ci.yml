name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  uv-example:
    name: python
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      
      - uses: astral-sh/ruff-action@v3
      - run: ruff check --fix
      - run: ruff format

      - name: Cache Python
        uses: actions/cache@v4
        with:
          path: ~/.local
          key: ${{ runner.os }}-python-3.13-${{ hashFiles('.python-version') }}
          restore-keys: |
            ${{ runner.os }}-python-3.13-
      
      - name: Install uv with cache
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Install the project
        run: uv lock && uv sync --locked --all-extras --dev

